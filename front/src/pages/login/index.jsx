import LoginForm from '../../components/login-form';

const Login = () => (
  <div>
    <h1>Please login:</h1>
    <LoginForm />
  </div>
);

Login.propTypes = {
};

Login.defaultProps = {
};


export default Login;
